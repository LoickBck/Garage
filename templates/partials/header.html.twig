<nav class="navbar navbar-expand-lg navbar-dark" style="background-color : #CAC0B3">
            <div class="container">
                <a class="navbar-brand" href="{{ path('homepage') }}" style="color: #001233">Silver Ride
                <img src="{{ asset('images/Logo.png') }}" alt="Logo" width="60"></a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>

                <div class="collapse navbar-collapse" id="navbarColor01">
                    <ul class="navbar-nav me-auto">
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('cars_index') }} "style="color: #FF595A" >Voitures</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="" style="color: #001233">Contact</a>
                        </li>
                    </ul>
                    <ul class="navbar-nav ms-auto">
                        {% if app.user %}
                            <li class="nav-item dropdown">
                                <a href="#" class="nav-link dropdown-toggle" data-bs-toggle="dropdown" aria-expanded="false" id="accountDropdownLink" style="color: #001233;background-color:#CAC0B3">{{app.user.fullName}}</a>
                                <div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDropdownLink" style="color: #001233;background-color:#CAC0B3">
                                    <a href="{{path('cars_create')}}" class="dropdown-item" style="color: #556B2F">Créer une présentation</a>
                                    <div class="dropdown-divider"></div>
                                    <a href="{{path('account_index')}}" class="dropdown-item"style="color: #001233">Mon compte</a>
                                    <a href="{{path('account_profile')}}" class="dropdown-item"style="color: #001233">Modifier mon profil</a>
                                    <div class="dropdown-divider"></div>
                                    <a href="{{path('account_logout')}}" class="dropdown-item" style="color: #FF595A">Déconnexion</a>
                                </div>
                            </li>
                        
                        {% else %}
                            <li class="nav-item">
                                <a href="{{path('account_register')}}" class="nav-link" style="color: #001233">Inscription</a>
                            </li>
                            <li class="nav-item">
                                <a href="{{path('account_login')}}" class="nav-link" style="color: #001233">Connexion</a>
                            </li>
                        {% endif %}
                        
                    </ul>
                </div>
            </div>
        </nav>
        {# dump(app.flashes) #}
        {% for label, messages in app.flashes %}
            <div class="container">
                <div class='alert alert-{{label}}'>
                    {% for message in messages %}
                        <p>{{message | raw}}</p>
                    {% endfor %}
                </div>
            </div>
        {% endfor %}