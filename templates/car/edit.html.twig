{% extends "base.html.twig" %}


{% block title %}Modification de la présentation: {{car.model}}{% endblock %}

{% form_theme myForm 'car/_collection.html.twig' %}

{% block body %}

    <div class="container">
        <h1>Modification de la présentation : <strong>{{car.model}}</strong></h1>
    
        {{form_start(myForm)}}

            {{form_widget(myForm)}}
            <button type="submit" class="btn btn-warning mt-3">Modifier</button>


        {{form_end(myForm)}}


    </div>

{% endblock %}

{% block javascripts %}
    {{encore_entry_script_tags('pres')}}
{% endblock %}